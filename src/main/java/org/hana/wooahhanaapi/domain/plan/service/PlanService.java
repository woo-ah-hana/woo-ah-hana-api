package org.hana.wooahhanaapi.domain.plan.service;

import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;




@Service
@RequiredArgsConstructor
public class PlanService {

//
//    public void createPlan(CreatePlanRequestDto dto) {
//        Plan plan = Plan.create(
//            null, dto.getCommunityId(), dto.getTitle(), dto.getStartDate(), dto.getEndDate(), dto.getCategory(), dto.getLocations(), dto.getMemberIds()
//        )
//    }
}
